spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/zlagoda?sslmode=disable
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  sql:
    init:
      mode: never

jwt:
  secret: test-secret-key-for-testing-purposes-only-minimum-256-bits-long
  expiration: 3600000
